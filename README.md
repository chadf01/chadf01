
<!---
chadf01/chadf01 is a ✨ special ✨ repository because its `README.md` (this file) appears on your GitHub profile.
You can click the Preview link to take a look at your changes.
---largeur = float(input("Entrez la largeur du parallélépipède : "))
hauteur = float(input("Entrez la hauteur du parallélépipède : "))
profondeur = float(input("Entrez la profondeur du parallélépipède : "))

volume = largeur * hauteur * profondeur
print(f"Le volume du parallélépipède est de {volume:.2f} unités cube.")
